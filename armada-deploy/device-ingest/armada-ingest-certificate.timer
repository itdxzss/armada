[Unit]
Description=Daily Armada device ingest certificate expiry check

[Timer]
OnCalendar=*-*-* 03:00:00 UTC
RandomizedDelaySec=1h
Persistent=true
Unit=armada-ingest-certificate.service

[Install]
WantedBy=timers.target
