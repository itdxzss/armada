# 接口协议

> 本文由 `.harness/wiki/parse_endpoints.py` + `.harness/wiki/format_api.py` 从 armada controller 生成。
> 如接口签名或 Javadoc 变化,请重新运行生成脚本。

---

# 一、公开 API
> 公开路径,通常不需要租户上下文。

## 租户登录 API（TenantAuthController）

### POST /api/public/auth/login

租户登录:校验租户码 + 密码,成功返回租户信息与占位 token。

- 权限：暂无接口级权限注解
- 请求体：`TenantLoginRequest`（登录入参(租户码 + 密码)）
- 响应 `data`：`TenantLoginVO`（登录结果(租户码、租户名、占位 token)）

---

# 二、租户业务 API
> 租户侧业务接口;租户上下文由 armada 后端统一处理。

## 账号分组 API（AccountGroupController）

### GET /api/account-groups

账号分组列表(分页 + 关键字筛选)。 同时懒创建系统默认分组(Service 层保证幂等)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | AccountGroupQuery | 否 | - | 查询条件 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（AccountGroupVO）`（分页分组列表）

### POST /api/account-groups

新建账号分组。

- 权限：暂无接口级权限注解
- 请求体：`AccountGroupDTO`（分组信息）
- 响应 `data`：`AccountGroupVO`（新建/复活的分组 VO）

### POST /api/account-groups/batch-delete

批量软删除账号分组(全或无闸门:组内有账号或命中系统组则整批拒删)。

- 权限：暂无接口级权限注解
- 请求体：`AccountIdsDTO`（分组 ID 列表）
- 响应 `data`：`Integer`（实际删除数）

### PUT /api/account-groups/{id}

修改账号分组基本信息(系统默认分组不可改名)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 路径中的分组 ID |

- 权限：暂无接口级权限注解
- 请求体：`AccountGroupDTO`（更新内容）
- 响应 `data`：无（仅 `code` / `message`）

## 账号列表 API（AccountController）

### GET /api/accounts

A1 账号分页列表(SQL 下推筛选)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | AccountQuery | 否 | - | 查询参数(分页 + 可选筛选字段) |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（AccountListVO）`（分页账号列表）

### POST /api/accounts/batch-delete

A7 批量软删除账号(全或无严格口径)。

- 权限：暂无接口级权限注解
- 请求体：`AccountIdsDTO`（账号 ID 列表）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/accounts/batch-migrate-group

A6 批量迁移分组。

- 权限：暂无接口级权限注解
- 请求体：`AccountMigrateGroupDTO`（迁移请求(ids + 目标分组 ID 或新分组名)）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/accounts/batch-offline

A5 批量发起账号下线(批量写入 outbox)。

- 权限：暂无接口级权限注解
- 请求体：`AccountIdsDTO`（账号 ID 列表）
- 响应 `data`：`AccountBatchOnlineVO`（outbox 批量下线命令受理汇总）

### POST /api/accounts/batch-online

A4 批量发起账号上线(后端逐账号分配空闲代理,批量写入 outbox)。

- 权限：暂无接口级权限注解
- 请求体：`AccountIdsDTO`（账号 ID 列表）
- 响应 `data`：`AccountBatchOnlineVO`（outbox 批量上线命令受理汇总）

### GET /api/accounts/stats

A2 账号统计卡(平台级聚合)。

- 权限：暂无接口级权限注解
- 响应 `data`：`AccountStatsVO`（统计卡数据(total/online/offline/banned/risk/assigned/unassigned)）

### POST /api/accounts/{id}/online

A3 发起单账号上线(后端自动分配空闲代理)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 账号 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`AccountOnlineVO`（outbox 上线命令受理回执）

### POST /api/accounts/{id}/probe

A4.2 主动探活账号。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 账号 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`AccountProbeVO`（探活结果）

### POST /api/accounts/{id}/refresh-status

A4.1 主动从协议层拉一次账号状态快照。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 账号 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`AccountStatusVO`（协议层状态快照）

## 账号导入 API（AccountImportController）

### GET /api/account-imports

C2 导入批次分页列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | AccountImportQuery | 否 | - | 查询条件(分页 + 可选筛选字段) |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（AccountImportBatchListVO）`（分页批次列表）

### POST /api/account-imports

C1 上传导入账号(multipart/form-data)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| form | query | AccountImportForm | 否 | - | 导入元信息表单(分组/格式/机型/类型/IP/备注/文本) |
| file | part | MultipartFile | 否 | - | 上传文件(可选;与 form.text 二选一) |

- 权限：暂无接口级权限注解
- 响应 `data`：`AccountImportBatchVO`（导入批次 VO(含计数统计及批次 ID)）

### GET /api/account-imports/{batchId}/details

C3 指定批次的导入明细分页列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| batchId | path | Long | 是 | - | 批次 ID |
| query | query | AccountImportDetailQuery | 否 | - | 查询条件(filter 可选,默认 all) |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（AccountImportDetailVO）`（分页明细列表）

### GET /api/account-imports/{batchId}/export

C4 按导入时的原始容器格式导出指定批次明细。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| batchId | path | Long | 是 | - | 批次 ID |
| scope | query | String | 否 | all | 结果范围:all(默认)/success/fail |

- 权限：暂无接口级权限注解
- 响应：`ResponseEntity<byte[]>`（文件响应）

## IP 代理 API（IpProxyController）

### GET /api/ip-proxies

分页查询 IP 代理列表：按国家 / 类型 / 来源 / 关键字组合筛选并分页，供列表页展示。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | IpProxyQuery | 否 | - | 搜索与分页条件 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（IpProxyVO）`（当前页代理及总数）

### POST /api/ip-proxies/batch-delete

批量删除 IP 代理（软删）。

- 权限：暂无接口级权限注解
- 请求体：`IpProxyBatchDeleteDTO`（选中的代理 ID 列表）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/ip-proxies/import

批量导入 IP 代理：逐行解析 host:port:用户名:密码，去重 + 格式校验后入库，返回导入统计。

- 权限：暂无接口级权限注解
- 请求体：`IpProxyImportDTO`（导入参数（国家、协议、来源、多行原文））
- 响应 `data`：`IpProxyImportResultVO`（导入结果）

### GET /api/ip-proxies/regions

查询本租户 IP 池已有国家/区域,供筛选框和账号导入选择 IP 国家使用。

- 权限：暂无接口级权限注解
- 响应 `data`：`List<String>`（去重后的国家/区域列表,「混合（不限国家）」优先）

## 群链接分组 API（GroupLinkLabelController）

### GET /api/group-link-labels

A1 分组列表(分页 + 关键字筛选)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | GroupLinkLabelQuery | 否 | - | 查询条件 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（GroupLinkLabelVO）`（分页分组列表）

### POST /api/group-link-labels

A2 新建分组。

- 权限：暂无接口级权限注解
- 请求体：`GroupLinkLabelDTO`（分组信息）
- 响应 `data`：`GroupLinkLabelVO`（新建/复活的分组 VO）

### POST /api/group-link-labels/batch-delete

A4 批量删除分组(级联软删群链接+导入批次)。

- 权限：暂无接口级权限注解
- 请求体：`GroupIdsDTO`（分组 ID 列表）
- 响应 `data`：`Integer`（实际删除数）

### PATCH /api/group-link-labels/{id}

A3 修改分组。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 路径中的分组 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupLinkLabelDTO`（更新内容）
- 响应 `data`：无（仅 `code` / `message`）

## 群组列表 API（GroupLinkController）

### GET /api/group-links

B2 群组列表主查询;labelId 可选,为空时查询当前租户全量群组列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | GroupLinkQuery | 否 | - | 查询条件(labelId/keyword/status/sourceFileName/origin/membershipState/page/pageSize) |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（GroupLinkVO）`（分页群链接列表）

### POST /api/group-links/batch-delete

B4 批量软删除群链接。

- 权限：暂无接口级权限注解
- 请求体：`GroupIdsDTO`（ID 列表）
- 响应 `data`：`Integer`（实际删除行数）

### POST /api/group-links/batch-preview

B5 批量实时预览群链接。

- 权限：暂无接口级权限注解
- 请求体：`GroupLinkPreviewDTO`（预览请求(accountId + ids)）
- 响应 `data`：`GroupLinkPreviewBatchVO`（批量预览汇总）

### POST /api/group-links/import

B1 导入群链接(multipart:text 手填 + 文件上传)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| labelId | query | Long | 是 | - | 目标WS链接分组 ID |
| batchName | query | String | 否 | - | 批次名称(来源文件/批次名称,非必填;留空存 NULL) |
| text | query | String | 否 | - | 手填多行文本(可选) |
| file | part | MultipartFile | 否 | - | 上传文件 TXT/CSV/Excel(可选) |

- 权限：暂无接口级权限注解
- 响应 `data`：`GroupLinkImportResultVO`（导入汇总结果）

### POST /api/group-links/migrate

B3 批量迁移群链接到目标分组。

- 权限：暂无接口级权限注解
- 请求体：`GroupLinkMigrateDTO`（迁移请求(linkIds + targetLabelId)）
- 响应 `data`：`Integer`（实际迁移行数）

### PATCH /api/group-links/{id}

更新群组列表本地资料。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupLinkProfileDTO`（本地群名称/备注/头像 URL）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/group-links/{id}/announcement-text

修改 WhatsApp 群公告文本。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupAnnouncementTextCommandDTO`（操作账号与公告文本）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/group-links/{id}/description

修改 WhatsApp 真实群描述。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupDescriptionCommandDTO`（操作账号与群描述）
- 响应 `data`：无（仅 `code` / `message`）

### GET /api/group-links/{id}/members

群组明细实时成员列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`GroupLinkMemberListVO`（协议层实时返回的成员列表;成员明细不落库）

### POST /api/group-links/{id}/picture

修改 WhatsApp 真实群头像。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupPictureCommandDTO`（操作账号与头像 URL/base64）
- 响应 `data`：无（仅 `code` / `message`）

### POST /api/group-links/{id}/subject

修改 WhatsApp 真实群名称。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 群链接 ID |

- 权限：暂无接口级权限注解
- 请求体：`GroupSubjectCommandDTO`（操作账号与新群名称）
- 响应 `data`：无（仅 `code` / `message`）

## 群链接导入批次 API（GroupLinkImportController）

### GET /api/group-link-imports/details

C1 导入明细分页列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | GroupLinkImportDetailQuery | 否 | - | 查询条件(labelId/batchId/result/failReason/page/pageSize;result=1成功、2失败) |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（GroupLinkImportDetailVO）`（分页明细 VO）

### GET /api/group-link-imports/failed/export

C2 导出失败明细(result=失败)为 CSV 文件。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| labelId | query | Long | 否 | - | 分组 ID(可选) |
| batchId | query | Long | 否 | - | 批次 ID(可选) |

- 权限：暂无接口级权限注解
- 响应：`void`

## 进群任务 API（JoinTaskController）

### GET /api/join-tasks

进群任务列表(分页 + 关键字/状态/分组/分配方式/间隔/时间筛选)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | JoinTaskQuery | 否 | - | 查询条件 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（JoinTaskVO）`（分页任务列表）

### POST /api/join-tasks

新建进群任务。

- 权限：暂无接口级权限注解
- 请求体：`CreateJoinTaskDTO`（建任务入参）
- 响应 `data`：`JoinTaskVO`（新建任务列表行视图）

### POST /api/join-tasks/batch-delete

批量软删任务。

- 权限：暂无接口级权限注解
- 请求体：`JoinTaskIdsDTO`（任务 id 列表）
- 响应 `data`：`Integer`（实际软删行数）

### GET /api/join-tasks/intervals

进群间隔下拉选项(去重)。

- 权限：暂无接口级权限注解
- 响应 `data`：`List<String>`（间隔标签列表）

### GET /api/join-tasks/{id}

任务详情(含 JSON 快照解析,供编辑回填)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`JoinTaskDetailVO`（任务详情）

### PUT /api/join-tasks/{id}

编辑任务(仅 DRAFT 且未执行)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 任务 ID |

- 权限：暂无接口级权限注解
- 请求体：`CreateJoinTaskDTO`（新配置）
- 响应 `data`：`JoinTaskDetailVO`（编辑后的任务详情）

### GET /api/join-tasks/{id}/results

任务明细行(每账号每链接一行)。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`List<JoinResultRowVO>`（明细行列表）

### POST /api/join-tasks/{id}/start

启动任务:把 DRAFT 任务转 RUNNING 并触发后台 worker 执行。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：无（仅 `code` / `message`）

## 营销模板 API（MarketingTemplateController）

### GET /api/marketing-templates

查询营销模板列表:按 ID / 模板名 / 文本类型 / 超链模式组合筛选并分页,供列表页展示。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | MarketingTemplateQuery | 否 | - | 搜索与分页条件 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（MarketingTemplateVO）`（当前页模板及总数）

### POST /api/marketing-templates

新增营销模板:接收表单配置,交 Service 校验并落库后返回新模板;校验失败以业务错误码返回。

- 权限：暂无接口级权限注解
- 请求体：`MarketingTemplateDTO`（模板配置）
- 响应 `data`：`MarketingTemplateVO`（创建后的模板）

### POST /api/marketing-templates/batch-delete

批量删除营销模板(软删)。

- 权限：暂无接口级权限注解
- 请求体：`BatchIdsRequest`（选中的模板 ID 列表）
- 响应 `data`：无（仅 `code` / `message`）

### PUT /api/marketing-templates/{id}

编辑指定营销模板,返回更新后的模板。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 模板 ID |

- 权限：暂无接口级权限注解
- 请求体：`MarketingTemplateDTO`（新的模板配置）
- 响应 `data`：`MarketingTemplateVO`（更新后的模板）

### POST /api/marketing-templates/{id}/clone

复制指定营销模板,生成名称带「副本」后缀的新模板。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 源模板 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`MarketingTemplateVO`（复制生成的新模板）

## 营销任务 API（MarketingTaskController）

### GET /api/marketing-tasks

查询营销任务列表。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| query | query | MarketingTaskQuery | 否 | - | 查询和分页参数 |

- 权限：暂无接口级权限注解
- 响应 `data`：`分页（MarketingTaskVO）`（营销任务分页列表）

### POST /api/marketing-tasks

新建营销任务。

- 权限：暂无接口级权限注解
- 请求体：`CreateMarketingTaskDTO`（新建营销任务入参）
- 响应 `data`：`MarketingTaskVO`（创建后的营销任务）

### GET /api/marketing-tasks/account-tree

查询建任务抽屉的账号→可营销群树。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| groupId | query | Long | 是 | - | 账号分组 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`MarketingAccountTreeVO`（账号→可营销群树）

### POST /api/marketing-tasks/batch-delete

批量软删营销任务。

- 权限：暂无接口级权限注解
- 请求体：`BatchIdsRequest`（任务 ID 列表）
- 响应 `data`：`Integer`（实际软删行数）

### GET /api/marketing-tasks/{id}

查询营销任务详情。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 营销任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`MarketingTaskDetailVO`（营销任务详情）

### PUT /api/marketing-tasks/{id}/marketing-template

通过任务修改其引用的营销模板。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 营销任务 ID |

- 权限：暂无接口级权限注解
- 请求体：`MarketingTemplateDTO`（新的营销模板配置）
- 响应 `data`：`MarketingTemplateVO`（更新后的营销模板）

### POST /api/marketing-tasks/{id}/start

启动营销任务。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 营销任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`MarketingTaskVO`（启动后的营销任务）

### POST /api/marketing-tasks/{id}/stop

停止营销任务。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| id | path | Long | 是 | - | 营销任务 ID |

- 权限：暂无接口级权限注解
- 响应 `data`：`MarketingTaskVO`（停止后的营销任务）

## Country API（CountryController）

### GET /api/admin/countries/options

国家下拉选项。scope=ip 时返回 MIXED 虚拟项 + 启用且 IP 可展示的真实国家。

| 参数 | 位置 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|------|
| scope | query | String | 否 | ip | 选项范围,当前支持 ip;为空时按 ip 处理 |

- 权限：暂无接口级权限注解
- 响应 `data`：`CountryOptionsVO`（国家选项列表）
