<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.marketing.asset.mapper.ResourceAssetGroupMapper">
    <select id="selectAll" resultType="com.armada.marketing.asset.model.vo.ResourceAssetGroupVO">
        SELECT id, group_name FROM resource_asset_group WHERE scope = #{scope} ORDER BY group_name, id
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO resource_asset_group (group_name, created_at, scope) VALUES (#{groupName}, #{createdAt}, #{scope})
    </insert>
    <select id="lockById" resultType="long">
        SELECT id FROM resource_asset_group WHERE id = #{id} AND scope = #{scope} FOR UPDATE
    </select>
    <delete id="delete">
        DELETE FROM resource_asset_group WHERE id = #{id}
    </delete>
    <delete id="ungroup">
        DELETE FROM resource_asset_group_ref WHERE group_id = #{groupId} AND scope = #{scope}
    </delete>
    <delete id="deleteRefs">
        DELETE FROM resource_asset_group_ref WHERE scope = #{scope} AND file_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
    <insert id="move">
        INSERT INTO resource_asset_group_ref (file_id, group_id, created_at, scope) VALUES
        <foreach collection="ids" item="id" separator=",">(#{id}, #{groupId}, #{now}, #{scope})</foreach>
    </insert>
</mapper>
