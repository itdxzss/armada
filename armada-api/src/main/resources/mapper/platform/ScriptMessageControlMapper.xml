<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.platform.protocol.mapper.ScriptMessageControlMapper">
 <sql id="identity">command_id=#{commandId} AND aggregate_type='SCRIPT_MARKETING_SEND_RECORD'</sql>
 <update id="hold">UPDATE protocol_command_outbox SET status=4, last_error='SCRIPT_PAUSED', locked_by=NULL, locked_at=NULL, updated_at=#{now} WHERE <include refid="identity"/> AND status IN (0,1)</update>
 <update id="resume">UPDATE protocol_command_outbox SET status=0, last_error=NULL, next_retry_at=#{now}, updated_at=#{now} WHERE <include refid="identity"/> AND status=4 AND last_error='SCRIPT_PAUSED'</update>
 <update id="expirePending">UPDATE protocol_command_outbox SET status=4, last_error='SCRIPT_EXPIRED', locked_by=NULL, locked_at=NULL, updated_at=#{now} WHERE <include refid="identity"/> AND (status IN (0,1) OR (status=4 AND last_error='SCRIPT_PAUSED'))</update>
 <update id="expireDispatching">UPDATE protocol_command_outbox SET status=6, last_error='SCRIPT_EXPIRED', updated_at=#{now} WHERE <include refid="identity"/> AND status=5</update>
</mapper>
