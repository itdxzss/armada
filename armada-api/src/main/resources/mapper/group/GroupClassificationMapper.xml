<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.group.mapper.GroupClassificationMapper">

  <!-- 账号控制事实可以建 canonical 群，但建档本身不能决定业务分类。 -->
  <insert id="ensureCanonicalGroup">
    INSERT INTO wa_group (
      tenant_id, group_jid, origin, group_classification, created_at, updated_at
    ) VALUES (
      #{tenantId}, #{groupJid}, 5, 0, #{now}, #{now}
    )
    ON DUPLICATE KEY UPDATE id = id
  </insert>

  <insert id="ensureCanonicalGroups">
    INSERT INTO wa_group (
      tenant_id, group_jid, origin, group_classification, created_at, updated_at
    ) VALUES
    <foreach collection="groupJids" item="groupJid" separator=",">
      (#{tenantId}, #{groupJid}, 5, 0, #{now}, #{now})
    </foreach>
    ON DUPLICATE KEY UPDATE id = id
  </insert>

  <!-- WHERE 条件是首次写唯一真相；事实时间只留证，不参与晚到事件追溯改写。 -->
  <update id="classifyFirst">
    UPDATE wa_group
    SET group_classification = #{classificationCode},
        group_classified_at = #{classifiedAt},
        group_classification_source = #{sourceCode},
        updated_at = GREATEST(updated_at, #{updatedAt})
    WHERE tenant_id = #{tenantId}
      AND group_jid = #{groupJid}
      AND group_classification = 0
      AND #{classificationCode} IN (1, 2)
      AND #{sourceCode} IN (1, 2)
  </update>

  <!-- 调用方先按 group_jid 做 FOR UPDATE current read，因此这批行均由当前事务持锁。 -->
  <update id="classifyFirstBatch">
    UPDATE wa_group
    SET group_classification = CASE group_jid
      <foreach collection="writes" item="write">
        WHEN #{write.groupJid} THEN #{write.classificationCode}
      </foreach>
      ELSE group_classification END,
        group_classified_at = CASE group_jid
      <foreach collection="writes" item="write">
        WHEN #{write.groupJid} THEN #{write.classifiedAt}
      </foreach>
      ELSE group_classified_at END,
        group_classification_source = CASE group_jid
      <foreach collection="writes" item="write">
        WHEN #{write.groupJid} THEN #{write.sourceCode}
      </foreach>
      ELSE group_classification_source END,
        updated_at = GREATEST(updated_at, #{updatedAt})
    WHERE tenant_id = #{tenantId}
      AND group_classification = 0
      AND group_jid IN
    <foreach collection="writes" item="write" open="(" separator="," close=")">
      #{write.groupJid}
    </foreach>
  </update>

  <select id="selectByGroupJids"
          resultType="com.armada.group.model.vo.CanonicalGroupClassificationRow">
    SELECT group_jid AS groupJid,
           group_classification AS classificationCode
    FROM wa_group
    WHERE tenant_id = #{tenantId}
      AND group_jid IN
    <foreach collection="groupJids" item="groupJid" open="(" separator="," close=")">
      #{groupJid}
    </foreach>
    ORDER BY group_jid ASC
    FOR UPDATE
  </select>

</mapper>
