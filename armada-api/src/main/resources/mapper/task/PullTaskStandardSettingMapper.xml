<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.task.mapper.PullTaskStandardSettingMapper">

  <resultMap id="standardSettingResultMap"
             type="com.armada.task.model.entity.PullTaskStandardSetting"
             autoMapping="true">
    <result column="is_clear_existing_members" property="clearExistingMembers"/>
    <result column="is_puller_join_by_link" property="pullerJoinByLink"/>
  </resultMap>

  <!-- tenant_id 由租户拦截器注入，列清单里不写。 -->
  <insert id="insert">
    INSERT INTO pull_task_standard_setting (
      task_id, auto_start, source_group_folder_id, source_group_folder_name,
      material_admin_timing, puller_sync_mode, is_clear_existing_members,
      is_puller_join_by_link,
      early_pull_count, early_pull_call_count,
      pull_count_min, pull_count_max,
      pull_interval_seconds, puller_count_per_group, station_count_per_call,
      concurrent_group_count, puller_risk_minutes, required_manager_count,
      manager_group_id, puller_group_id, station_group_id,
      manager_finish_group_id, puller_finish_group_id,
      manager_group_name, puller_group_name, station_group_name,
      manager_finish_group_name, puller_finish_group_name,
      created_at, updated_at
    ) VALUES (
      #{taskId}, #{autoStart}, #{sourceGroupFolderId}, #{sourceGroupFolderName},
      #{materialAdminTiming}, #{pullerSyncMode}, #{clearExistingMembers},
      #{pullerJoinByLink},
      #{earlyPullCount}, #{earlyPullCallCount},
      #{pullCountMin}, #{pullCountMax},
      #{pullIntervalSeconds}, #{pullerCountPerGroup}, #{stationCountPerCall},
      #{concurrentGroupCount}, #{pullerRiskMinutes}, #{requiredManagerCount},
      #{managerGroupId}, #{pullerGroupId}, #{stationGroupId},
      #{managerFinishGroupId}, #{pullerFinishGroupId},
      #{managerGroupName}, #{pullerGroupName}, #{stationGroupName},
      #{managerFinishGroupName}, #{pullerFinishGroupName},
      #{createdAt}, #{updatedAt}
    )
  </insert>

  <select id="selectByTaskId" resultMap="standardSettingResultMap">
    SELECT *
    FROM pull_task_standard_setting
    WHERE task_id = #{taskId}
  </select>

  <update id="freezeRequiredManagerCount">
    UPDATE pull_task_standard_setting
    SET required_manager_count = #{requiredManagerCount},
        updated_at = #{now}
    WHERE task_id = #{taskId}
  </update>

</mapper>
