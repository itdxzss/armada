# 变更记录：拉群数据包模型与菜单复刻范围

- 日期 / 分支 / worktree：2026-09-15 / `1.0.3-snapshot` / `/Users/daishuaishuai/IdeaProjects/armada`
- 需求来源：用户要求先做拉群数据包，询问新增模型及本次菜单复刻程度；竞品优先普通版。
- 状态：用户已明确“开始实现吧”；后端资源、现有任务接入和前端菜单已实现，已完成本地验证，未部署。
- 设计：`docs/business/group-pull-data-package-design.md`。
- 前后端合同：`docs/business/group-pull-data-package-api-contract.md`。

## 目标

按普通版竞品逐项对照拉群数据包菜单，实现可追溯的资源管理与正式拉群消费闭环，避免之前仅做资源子集和固定空态的验收缺口。

## 缺口拆解 / 任务清单

- [x] 读取数据模型规范、现有模型wiki、相关设计及当前代码。
- [x] 盘点竞品菜单字段、筛选、弹窗、导入/导出、隐私过滤、趋势、任务引用。
- [x] 核查超链四表的复用边界及当前正式拉群接入点。
- [x] 形成四表资源聚合推荐、现有任务事实复用与逐项验收设计。
- [x] 实现资源模型、迁移、接口和真实数据库测试。
- [x] 实现动态菜单、筛选、表格、全部弹窗和下载。
- [x] 接入正式标准拉群的来源、领取、回写、释放和换群重试。
- [x] 完成历史隐私过滤。
- 点击归因/访问趋势：用户已明确选择后续接入，不属于本次完成范围。
- [x] 完成本地浏览器7项和真实任务/资源同库事务验收；实际WhatsApp/远程环境尚未验证。

## 关键设计决策

- 用户已确认独立四表方案：`group_data_package` / `group_data_package_phone` / `group_data_package_import` / `group_data_package_stat`。
- 用户明确要求“不要过度设计”：只落当前有读写用途的字段，沿用 Controller → Service → Mapper 与现有任务链路，不建设通用框架、额外执行日志、独立对账调度器或预留指标。
- 超链状态和领取深度绑定其业务；本次不修改超链四表为通用混合号码池。
- 现有任务成员和协议尝试仍为执行事实；来源引用和分配版本穿过换群重试，不新增第二套执行日志。
- 保留A/a管理员、原始顺序及料子执行单元；UNKNOWN不随失败重置回收。
- 用户在实现中明确点击趋势延期，本次完成菜单及拉群取用/回写；历史隐私过滤仍在本次范围内。
- 用户确认四表不等于确认每个业务细节；其他适配保持显式记录，不擅自扩大功能范围。

## 验证

- 三个独立只读分析分别核对竞品菜单、后端模型、前端接入。
- 当前已读取普通版前端、V153/V158及现有实现；模型wiki漏收超链表，以迁移/代码补证。
- 已完成本地实现；实测记录见 `docs/business/group-pull-data-package-implementation.md`。
- 验收分层：真实Mapper/H2事务、前端行为测试、合成API浏览器交互；不将这三层等同于已部署或WhatsApp实机执行。

## 部署

- 未提交、未部署、未连接真实数据库或远程环境。

## 遗留 / 跟进

- 迁移采用V191/V192，审计副本与保护回滚位于 `.harness/changes/group-pull-data-package/`。上线前仍需核对目标分支是否新增同号迁移。
- 访问趋势需要实际群任务消息和包来源归因；现有超链访问趋势固定空态不能用作实现。
- 已运行 `python3 .harness/wiki/gen_datamodel.py`；缺少 `/tmp/wheel_tables.tsv` 而退出，自动wiki未刷新。当前四表/来源字段以V191/V192及本文档为准，不伪造真库导出，不手改生成wiki。

- 最终后端合并25类249项通过；前端166项业务+11项菜单路由测试与构建通过；浏览器7项通过。两项基线既有E2E差异独立复现并排除，未修改断言。
