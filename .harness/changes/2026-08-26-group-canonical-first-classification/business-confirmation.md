# 可直接回复的业务确认

- `change_id`: `2026-08-26-group-canonical-first-classification`
- 当前确认状态: `CONFIRMED`

## 确认消息

这次需求建议按下面口径实施。请回复 **“全部按 A”**，或只写需要调整的编号，例如 **“D2-03=C，D2-04=B”**：

1. `D2-01=A`：同一 WhatsApp 群按租户独立定类，不跨租户共享。
2. `D2-02=A`：只有首次完整 baseline 能定历史群；baseline 后可靠 self-add/完整快照新增能定上控后群；链接导入和预览不定类。
3. `D2-03=A`：第一个成功写入的可靠分类获胜，之后永久不变；不因晚到的更早时间戳追溯改类。
4. `D2-04=A`：旧数据先 dry-run；能证明先后的取最早事实，无法证明的双分类保守归历史群并进入审计报告。
5. `D2-05=A`：页面本期只显示一个分类并删除“同时属于两类”；API 新增单枚举，旧双布尔只读兼容一个发布窗口且保证互斥。
6. `D2-06=A`：当前提出人作为业务验收 owner；成功标准是零新增双分类、首次写后不变、迁移可审计、页面单标签。

这次确认只冻结产品口径，不代表授权远程动作。等本地实现与测试完成后，我会再给一份具体的 test1 安全信封，单独申请部署、Flyway、测试账号事件、Runner soak/CloudWatch 与 IAM 权限。

## 确认记录

| 时间 | 确认人 | 结论 | `scope_hash` |
|---|---|---|---|
| 2026-08-26 15:01:51 CST | 当前提出人 | 全部按 A | `316839dc898e558b494ff6835abf15cd55d942e8a730c927ba76a6c25be61825` |
