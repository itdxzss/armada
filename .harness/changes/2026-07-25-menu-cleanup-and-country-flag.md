# 菜单清理与账号国旗恢复

## 变更

- 新增 Flyway V076：仅在 `GroupManagement` 没有子节点时，先清理角色关联，再删除该无业务含义的空目录。
- 保留并核对 `AccountManagement -> AccountGroup`，账号分组页面和后端 CRUD 未重复开发。
- 合入遗漏在 `dev_hhw` 的账号国家国旗后端提交；混合国家或未匹配国家不返回国旗。
- 前端同步合入国旗展示，并清空登录页硬编码的默认用户名。

## 验证与部署

- 后端菜单迁移测试、账号转换测试和打包通过。
- 前端登录页测试、账号表格测试、类型检查和生产构建通过。
- 2026-07-25 仅部署到第一套测试环境 `test1`：后端 `43d6284`，前端 `d9ef1b3`；Protocol、Zhuan 均未部署。
- Flyway V076 执行成功；`GroupManagement` 数量为 0，`AccountGroup` 仍启用且父节点为 `AccountManagement`。
- 后端容器重启次数为 0；公开验证码接口返回 200，未登录业务接口返回 401。
