# 变更记录：超链任务六方案 test1 验收案例

- 日期 / 分支 / worktree: 2026-08-28 / `1.0.3-snapshot` / 主工作区
- 需求来源: 用户要求根据新提交的六份超链任务设计同步产出部署后测试案例
- 状态: 已完成测试设计，尚未执行

## 目标（一句话）

把公共契约及 H1～H6 设计转换为可绑定候选版本、可在 test1 执行、可留证且 fail-closed 的严格验收测试套件。

## 缺口拆解 / 任务清单

- [x] 同步远端设计提交 `03aeca98`
- [x] 完整读取公共契约和六份设计
- [x] 核对当前代码尚未实现 `/api/hyperlink-tasks` 新合同
- [x] 定义 quick/integration/canary/soak/perf 分层和严格判定
- [x] 定义 test1 夹具、安全信封、证据与 cleanup 合同
- [x] 为公共契约和 H1～H6 编写可执行 Given/When/Then 案例
- [x] 补四条跨方案端到端主旅程和最终放行门禁
- [ ] 实施 `integration` Runner profile 与案例执行器
- [ ] 功能开发完成并冻结 scope_hash、测试束和四仓候选版本
- [ ] 获得 test1 D3 授权后执行并生成证据包

## 关键设计决策

- `quick` 只证明版本、页面只读主路径和运行态健康，不代表业务验收。
- 部署后业务验收以 `integration` 为主，真实 Web/Android 私聊分别使用受控 canary，恢复和统计另跑 60 分钟 soak。
- required 案例缺执行器、夹具或证据时返回 BLOCKED，不允许 SKIPPED 或用默认值伪造 PASS。
- 案例直接绑定设计提交和七份设计文件的组合 SHA-256；需求口径改变必须产生新套件版本。
- 当前只交付测试设计，不在生产代码加入 mock/测试入口，也未部署或连接 test1 真库。

## 验证（evidence-before-done）

已完成以下文档验证：

```text
测试案例定义数：97
重复案例 ID：0
设计证据 E-H1-01～E-H6-20 与追踪矩阵差异：0
引用但未定义的案例 ID：0
七份设计文件链接缺失：0
设计束 SHA-256：434757e6c2d1565bea0fddc57a380b4a888c5aca9a87cce972449bc16df09ca4
敏感凭据模式扫描：0 命中
git diff --check：PASS
```

## 部署

- commit / 环境 / 部署后验证结果: 未部署；本次仅新增测试设计文档。

## 遗留 / 跟进

- 六份设计状态均为待实施/冻结候选，当前候选接口缺失；测试结果必须保持 NOT_RUN。
- 实施功能时应同步实现案例执行器，不能等部署后再临时编写测试。
- 执行 real canary 前必须确认 test1、真实账号资源别名、消息上限和 cleanup 安全信封。
