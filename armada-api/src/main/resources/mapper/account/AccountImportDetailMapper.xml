<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.account.mapper.AccountImportDetailMapper">

  <!--
    批量插入明细行;tenant_id 由拦截器注入。
    batchInsert 用 foreach 生成多值 INSERT,一次 SQL 写多行。
  -->
  <insert id="batchInsert">
    INSERT INTO account_import_detail (
      batch_id, line_no, ws_phone, account_id,
      parse_result, fail_reason, login_result, created_at
    ) VALUES
    <foreach collection="rows" item="r" separator=",">
      (
        #{r.batchId}, #{r.lineNo}, #{r.wsPhone}, #{r.accountId},
        #{r.parseResult}, #{r.failReason}, #{r.loginResult}, #{r.createdAt}
      )
    </foreach>
  </insert>

</mapper>
