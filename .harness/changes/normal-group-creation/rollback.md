# 回滚说明

代码回滚时先停止 Web/Android 普通群命令消费者和 Armada 普通群结果消费者，避免旧代码读取
新命令。保留 V101/V102 表结构和已有数据，不执行破坏性降级；将入口关闭并等待已发送命令完成
对账。若必须物理删除 V102 列/索引，应先确认所有普通群任务终态、备份数据，并通过新的
Flyway 版本迁移执行，禁止手工修改共享库或回写已经执行过的 V102。

V113 上线后回滚应用时同样保留次管理员任务列和明细表；先关闭新建普群入口，等待已经下发的
建群命令完成并核对提权结果，再回滚应用。已建群但提权失败的 `CREATED_PARTIAL` 记录必须保留
`groupJid` 和失败明细供人工处理，禁止通过重试建群来“修复”。
