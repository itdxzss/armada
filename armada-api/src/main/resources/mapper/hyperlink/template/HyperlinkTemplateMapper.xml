<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.hyperlink.template.mapper.HyperlinkTemplateMapper">

    <sql id="Columns">
        id, tenant_id, template_name, message_type, message_schema_version, title,
        content, link_description, promotion_link, buttons, card_text,
        link_preview_asset_id, body_main_asset_id, remark, version, created_by,
        created_at, updated_at, deleted_at
    </sql>

    <sql id="FilterWhere">
        <where>
            deleted_at IS NULL
            <if test="q.name != null and q.name != ''">
                AND template_name LIKE CONCAT('%', #{q.name}, '%')
            </if>
            <if test="q.messageType != null">
                AND message_type = #{q.messageType}
            </if>
            <if test="q.createdFrom != null">
                AND created_at &gt;= #{q.createdFrom}
            </if>
            <if test="q.createdTo != null">
                AND created_at &lt;= #{q.createdTo}
            </if>
        </where>
    </sql>

    <select id="countPage" resultType="long">
        SELECT COUNT(*)
        FROM hyperlink_template
        <include refid="FilterWhere"/>
    </select>

    <select id="selectPage" resultType="com.armada.hyperlink.template.model.entity.HyperlinkTemplate">
        SELECT <include refid="Columns"/>
        FROM hyperlink_template
        <include refid="FilterWhere"/>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{q.pageSize} OFFSET #{q.offset}
    </select>

    <select id="selectById" resultType="com.armada.hyperlink.template.model.entity.HyperlinkTemplate">
        SELECT <include refid="Columns"/>
        FROM hyperlink_template
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="selectOptions" resultType="com.armada.hyperlink.template.model.entity.HyperlinkTemplate">
        SELECT <include refid="Columns"/>
        FROM hyperlink_template
        WHERE deleted_at IS NULL
        <if test="messageType != null">AND message_type = #{messageType}</if>
        <if test="keyword != null and keyword != ''">
            AND (template_name LIKE CONCAT('%', #{keyword}, '%')
                OR title LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="existsByName" resultType="boolean">
        SELECT COUNT(*) &gt; 0
        FROM hyperlink_template
        WHERE template_name = #{name} AND deleted_at IS NULL
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <insert id="insert" parameterType="com.armada.hyperlink.template.model.entity.HyperlinkTemplate"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hyperlink_template
            (template_name, message_type, message_schema_version, title, content,
             link_description, promotion_link, buttons, card_text, link_preview_asset_id,
             body_main_asset_id, remark, version, created_by, created_at, updated_at)
        VALUES
            (#{templateName}, #{messageType}, #{messageSchemaVersion}, #{title}, #{content},
             #{linkDescription}, #{promotionLink}, #{buttons}, #{cardText}, #{linkPreviewAssetId},
             #{bodyMainAssetId}, #{remark}, #{version}, #{createdBy}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateByIdAndVersion">
        UPDATE hyperlink_template
        SET template_name = #{entity.templateName},
            message_type = #{entity.messageType},
            message_schema_version = #{entity.messageSchemaVersion},
            title = #{entity.title},
            content = #{entity.content},
            link_description = #{entity.linkDescription},
            promotion_link = #{entity.promotionLink},
            buttons = #{entity.buttons},
            card_text = #{entity.cardText},
            link_preview_asset_id = #{entity.linkPreviewAssetId},
            body_main_asset_id = #{entity.bodyMainAssetId},
            remark = #{entity.remark},
            version = version + 1,
            updated_at = #{entity.updatedAt}
        WHERE id = #{entity.id}
          AND version = #{expectedVersion}
          AND deleted_at IS NULL
    </update>

    <update id="softDelete">
        UPDATE hyperlink_template
        SET deleted_at = #{deletedAt}, updated_at = #{deletedAt}
        WHERE id = #{id} AND deleted_at IS NULL
    </update>
</mapper>
