<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.armada.hyperlink.task.mapper.HyperlinkTaskContentMapper">
    <sql id="Columns">hyperlink_task_id, tenant_id, message_schema_version, message_type, title,
      content, link_description, promotion_link, buttons, card_text, link_preview_asset_id,
      body_main_asset_id, created_at, updated_at</sql>
    <insert id="insert">
        INSERT INTO hyperlink_task_content
        (hyperlink_task_id, message_schema_version, message_type, title, content, link_description,
         promotion_link, buttons, card_text, link_preview_asset_id, body_main_asset_id, created_at, updated_at)
        VALUES (#{hyperlinkTaskId}, #{messageSchemaVersion}, #{messageType}, #{title}, #{content},
         #{linkDescription}, #{promotionLink}, #{buttons}, #{cardText}, #{linkPreviewAssetId},
         #{bodyMainAssetId}, #{createdAt}, #{updatedAt})
    </insert>
    <select id="selectByTaskId" resultType="com.armada.hyperlink.task.model.entity.HyperlinkTaskContent">
        SELECT <include refid="Columns"/> FROM hyperlink_task_content WHERE hyperlink_task_id=#{taskId}
    </select>
    <update id="update">
        UPDATE hyperlink_task_content SET message_schema_version=#{messageSchemaVersion},
          message_type=#{messageType}, title=#{title}, content=#{content},
          link_description=#{linkDescription}, promotion_link=#{promotionLink}, buttons=#{buttons},
          card_text=#{cardText}, link_preview_asset_id=#{linkPreviewAssetId},
          body_main_asset_id=#{bodyMainAssetId}, updated_at=#{updatedAt}
        WHERE hyperlink_task_id=#{hyperlinkTaskId}
    </update>
</mapper>
