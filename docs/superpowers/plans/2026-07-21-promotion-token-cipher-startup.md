# 推广 Token 加密组件启动修复实施计划

1. 在 `PromotionTokenCipherTest` 中增加真实 Spring 上下文创建测试，确认当前代码稳定复现构造器选择失败。
2. 在生产构造器上增加明确的 Spring 注入标记，保留原有测试构造器并跑定向 Java 测试。
3. 先为测试/生产部署模板补充配置契约断言，再为 Compose 和 `.env.example` 增加变量透传、必填约束与占位说明。
4. 在测试、Windows 和生产部署入口校验密钥格式与长度，并将 `.env` 权限收紧为 `0600`。
5. 运行定向 Java 测试、部署脚本测试和必要的编译验证。
6. 完成 Java 与敏感配置安全评审，仅将本次文件暂存到 IDEA Commit 区，不创建提交。
